{
  "private": true,
  "name": "spytec-saber-website",
  "license": "MIT",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d public -t",
    "dev": "saber",
    "build": "saber build"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "saber": "^0.6.10",
    "saber-plugin-query-posts": "^0.3.2",
    "saber-theme-portfolio": "^0.2.0"
  },
  "dependencies": {
    "saber-plugin-feed": "^0.3.4"
  }
}
